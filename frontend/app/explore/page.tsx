import ExploreView from "@/features/explore/ExploreView";

/**
 * The destination for the Hero's "Explore LifeOS" link (see
 * features/explore/ExploreView.tsx for why this is a dedicated page
 * rather than a same-page anchor scroll).
 */
export default function ExplorePage() {
  return <ExploreView />;
}

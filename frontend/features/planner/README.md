# features/planner

UI for goal-to-plan creation: the screen(s) where a user states a Goal and
reviews/edits the AI-generated plan before it moves to approval and
execution (see `docs/architecture.md` for the full flow).

Placeholder only - no components implemented yet.

# features/dashboard

The main dashboard: cross-workspace progress tracking, active plans, and
pending approvals. Distinct from `app/page.tsx`, which is the marketing/
landing entry point - this is the authenticated home screen.

Implemented (Sprint 1 Day 3): `DashboardView`, `GoalCard`, `CreateGoalModal`.

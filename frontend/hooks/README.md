# hooks

Shared React hooks (e.g. `useWorkspace`, `useApproval`, `usePlan`) used
across features. Kept separate from `features/*` so hooks that span more
than one feature area have an obvious home.

Placeholder only - no hooks implemented yet.

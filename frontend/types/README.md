# types

Shared TypeScript types/interfaces for core domain objects (Goal,
Workspace, Plan, Task, Agent, ApprovalRequest) used across `features/`,
`services/`, and `components/`.

`goal.ts` and `workspace.ts` are implemented (Sprint 1 Day 3). Additional
types (Plan, Task, Agent, ApprovalRequest) will land as those features do.
